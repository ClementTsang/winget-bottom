name: release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "The version to deploy"
        required: true
        type: string

jobs:
  create-release:
    name: "Create Release"
    runs-on: windows-latest
    steps:
      - uses: ClementTsang/winget-releaser@87999d06e7cc62c10808c914c9d456e7c15b5c7b
        with:
          identifier: Clement.bottom
          installers-regex: '^bottom_x86_64_installer\.msi$'
          version: ${{ github.event.inputs.version }}
          release-tag: ${{ github.event.inputs.version }}
          token: ${{ secrets.WINGET_TOKEN }}
          release-repository: "bottom"
